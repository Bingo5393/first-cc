# 朋友圈转发助手 —— 依赖清单
# 安装：pip install -r requirements.txt
#
# 注意：wcferry 需要配合对应版本的 PC 微信（3.9.6.0）和 WeChatFerry 的 wcf DLL 使用，
# 详见 README。

wcferry==39.6.0.0        # WeChatFerry Python 客户端（获取朋友圈/下载媒体）
loguru>=0.7.0            # 日志（按天轮转）
schedule>=1.2.0          # 定时任务调度
PyYAML>=6.0              # 解析 config.yaml
requests>=2.31.0         # 下载图片/视频
pywinauto>=0.6.8         # PC 微信 UI 自动化（发布朋友圈）
Pillow>=10.0.0           # 图像处理 / 截图辅助
